// react-dom-client-instance.js — the hosted import map's `react-dom/client`
// target. Explicit named re-exports (see react-instance.js). (Epic 10 / H1.2.)
export { createRoot, hydrateRoot } from 'react-dom/client';
