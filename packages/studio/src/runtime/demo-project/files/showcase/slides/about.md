# Slides

A talk deck, in code. A 16:9 title slide, a section divider that renders **three
slides from one file** (`section-divider.data.json` supplies each section's
number + title), and a closing "thanks" slide. Present straight from the canvas,
or export every slide to PNG.
