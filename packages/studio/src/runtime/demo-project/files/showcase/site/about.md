# Site

Web assets for a project site. A 1200×630 Open Graph card for link previews, a
`/now` page banner, and a favicon source you export to PNG at any size with
`@lerret/cli export`. One folder, every image your site needs.
