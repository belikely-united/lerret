# Live

Designs that refresh themselves. A co-located `.config.json` with
`{ "autoRefresh": 1000 }` re-renders an artboard on a timer — here a clock
ticking once a second and a counter racing ten times a second. Perfect for
dashboards, "now playing" cards, or live event screens.
