# Social

One brand, every format. These all read the same brand tokens
(`config.json` → `vars`), so changing the brand color re-tints the whole set at
once. An Instagram square, an Open Graph card (with a data-driven *launch*
variant), and a quote card — each at its real pixel size.
