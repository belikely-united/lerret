# Brand

The foundation every other design pulls from. The **logo** lockup and the
**brand tokens** (colors straight from `config.json` → `vars`) define the look;
the **business card** shows it applied — one component driven by data
(`business-card.data.json`) that renders a blank template *and* a filled card,
with a validation badge when a required field is missing.
