// react-dom-instance.js — the hosted import map's `react-dom` target (e.g. an
// asset importing `createPortal`). Explicit named re-exports (see
// react-instance.js). (Epic 10 / Story H1.2.)
export { createPortal, flushSync, version } from 'react-dom';
export { default } from 'react-dom';
